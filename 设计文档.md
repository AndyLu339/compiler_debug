# ToyC 编译器设计文档

## 整体流水线

```
[ C 源代码 ]
     │  lexer (ocamllex) → parser (Menhir)
     ▼
[ AST ]                          ← 前端中间形态 (AST.ml)
     │  semant (语义分析)
     ▼
[ 已验证的 AST ]
     │  irgen (AST → IR 内存模型)
     ▼
[ IR 内存模型 ]          ← alloca / load / store
     │  mem2reg (内存 → SSA)
     ▼
[ IR SSA 形式 ]          ← 纯寄存器 + phi 节点
     │  optimize (IR 优化 pass)
     ▼
[ 优化后的 IR ]
     ├── live_var (活变量分析) ──────────┐
     └── live_intervals (活跃区间分析) ─┐ │   ← 姊妹 pass, 独立计算
          │                             │ │
          ▼                             │ │
[ 活跃区间 ]                            │ │
     │  regalloc (寄存器分配)            │ │
     ▼                                  │ │
[ alloc_result ]                        │ │
     └────────────────────────────────┐ │ │
           ┌──────────────────────────┘ │ │
           ▼                            ▼ ▼
           codegen (IR → RISC-V)
           ▼
[ RISC-V 汇编 (.s) ]
```

---

## 各阶段

### 1. 词法分析 (`lexer.mll`)

- 输入：源文件字符流
- 输出：token 流
- 识别关键字 (`int` `void` `const` `if` `else` `while` `break` `continue` `return`)、标识符、整数常量、运算符、分隔符
- 跳过空白字符、`//` 单行注释、`/* */` 多行注释（支持嵌套）
- 整数常量不含负号：`-42` 由 parser 的一元 `-` + `42` 组合而成

### 2. 语法分析 (`parser.mly`)

- 输入：token 流
- 输出：`AST.comp_unit` — `(TopDecl | TopFunc)+`
- Menhir LALR(1) 解析，运算符优先级通过 `%left`/`%right`/`%nonassoc` 声明
- 表达式：`||` < `&&` < 关系 < `+` `-` < `*` `/` `%` < 一元 `!` `-` `+`

### 3. 语义分析 (`semant.ml`)

- 符号表 (`Symtab.ml`)：变量/常量/函数/形参的声明绑定，嵌套作用域
- 类型检查：表达式类型推导，`int`/`void` 返回一致性
- 约束检查：
  - `break`/`continue` 只能在 `while` 内
  - `void` 函数调用不能作为条件或赋值右值
  - `const` 不可赋值，初始化表达式仅含字面量和已声明常量
  - 函数必须先声明后调用（递归除外）
  - 入口 `main`：无参数，返回 `int`
- 返回值路径检查：`int` 函数所有路径都必须 `return`

### 4. IR 生成 (`irgen.ml` — 待写)

AST → IR (内存模型)。每条 ToyC 语句/表达式翻译为三地址码指令序列。

- **表达式展平**：
  - `a + b * c` → `%t0 = mul b, c` ; `%t1 = add a, %t0`
  - 短路求值 `a && b` → 条件分支跳转
  - 函数调用 `f(a, 1)` → `call @f(%a, 1)`

- **变量内存模型**：每个源级变量 `alloca` 一个槽位，读写走 `load`/`store`
  - `int x = 1;` → `%r0 = alloca i32` ; `store 1, %r0`
  - `x = x + 1;` → `%r1 = load %r0` ; `%r2 = add %r1, 1` ; `store %r2, %r0`

- **控制流翻译**：
  - `if (c) A else B` → 条件 `br` + 合并块
  - `while (c) S` → 循环头 + 条件 `br` + 循环体 + `jmp` 回边 + 出口块
  - `break` → `jmp` 到最近循环出口
  - `continue` → `jmp` 到最近循环条件检查
  - `return e` → `ret %e`

### 5. mem2reg (`mem2reg.ml` — 待写)

内存模型 IR → SSA 形式 IR。消除 `alloca`/`load`/`store`，转为纯寄存器 `phi` 节点。

1. 识别可提升的 `alloca` 槽位（ToyC 无取地址，全部可提升）
2. 计算支配树与支配边界
3. 在支配边界插入 `phi` 指令
4. 重命名：`load` → 直接引用当前版本 vreg，`store` → 产生新版 vreg
5. 删除死掉的 `alloca`/`load`/`store`

效果：4 条内存指令（`alloca` + `store` + `load` + `store`）→ 纯 `phi` + 算术。

### 6. IR 优化 (`lib/opt/` — 待写)

SSA 形式 IR → 优化后的 IR。13 个 pass，对齐 LLVM 的 pass 顺序，每个 pass 接口 `module_ → module_`。

| # | Pass | 文件 | LLVM 对应 | 功能 |
|---|---|---|---|---|
| 1 | 常量折叠 | `const_fold.ml` | — | Binop/Icmp 两侧都是 Imm → 直接折叠为 Imm |
| 2 | 重关联 | `reassociate.ml` | `-reassociate` | (a+C1)+C2 → a+(C1+C2)，聚集常量暴露折叠机会 |
| 3 | 常量折叠 | (再跑一轮) | — | reassociate 暴露的新常量 |
| 4 | 代数化简 | `algebraic.ml` | `-instcombine` | x+0→x, x*1→x, x*0→0, x-x→0 等 |
| 5 | Copy 传播 | `copy_prop.ml` | — | Copy dst, src → dst 的 use 全部替换为 src |
| 6 | 公共子表达式消除 | `cse.ml` | `-early-cse` | 相同操作数相同运算 → 重用第一个结果 |
| 7 | 死代码消除 | `dce.ml` | `-dce` | 基于 def-use：无 uses 的 vreg 删除定义指令 |
| 8 | 常量传播 | `const_prop.ml` | — | Copy dst, Imm → 把 dst 的所有 use 替换为 Imm |
| 9 | 稀疏条件常量传播 | `sccp.ml` | `-sccp` | SSA 格传播，穿越分支传播常量 |
| 10 | 无用分支消除 | `branch_fold.ml` | `-simplifycfg` | Br Imm 0/1 → Jump, 删除不可达块 |
| 11 | 循环不变量外提 | `licm.ml` | `-licm` | 循环内不变指令提到循环前 |
| 12 | 循环展开 | `loop_unroll.ml` | `-loop-unroll` | 小循环（固定次数）展开减少分支 |
| 13 | 死代码消除 | (再跑一轮) | — | 清理循环变换产生的死代码 |
| 14 | 代数化简 | (再跑一轮) | `-instcombine` | 循环变换后再次化简 |
| 15 | 尾递归消除 | `tail_call_elim.ml` | `-tailcallelim` | 尾递归 → jump 循环 |
| — | 死 store 消除 | `dead_store_elim.ml` | `-dse` | 基于活变量，省去写入已死 vreg 的 store |

**执行顺序**（对齐 LLVM pipeline）：
```
ConstFold → Reassociate → ConstFold → Algebraic → CopyProp
→ CSE → DCE → ConstProp → SCCP → BranchFold
→ LICM → LoopUnroll → DCE → Algebraic → TailCallElim
```

### 7. 活变量分析 (`live_var.ml`)

经典后向数据流分析。求解每个基本块的 `live_in` / `live_out` 集合。与 `live_intervals` 是姊妹 pass——独立计算，互不依赖。

- 数据结构：`liveness = { live_in : IntSet.t IntMap.t; live_out : IntSet.t IntMap.t }`
- 方程：`live_in = use ∪ (live_out − def)`，`live_out = ∪ live_in[succ]`
- 逆序遍历基本块迭代到不动点（快速收敛）
- 正确计算后继块 phi 入边的 use：若块 B 的后继有 phi `%r = phi [%rx, B]`，则 `%rx` 计入 B 的 use
- 输出传给 `codegen.generate`，用于指令级别判定 vreg 是否死亡等优化决策

### 8. 活跃区间分析 (`live_intervals.ml`)

基于 def-use 信息，先线性化函数（每个指令全局编号），再为每个 vreg 计算 `[start, stop]` 区间。**不依赖** `live_var` 的输出，直接从 def-use + 线性化独立计算（与 LLVM 的 `LiveIntervalCalc` 一致）。

- 数据结构：`interval = { vreg : int; start : int; stop : int }`
- 线性化：遍历基本块，phi 定义在块起始，非终结指令顺序编号，terminator 为块末尾
- phi 使用在 predecessor 的 terminator 位置
- 输出传给 `regalloc.allocate`，供线性扫描寄存器分配使用

### 9. 寄存器分配 (`regalloc.ml` — 待写)

SSA IR → 带分配信息的 IR。线性扫描（Linear Scan）算法。

- 数据结构：`alloc_result = { vreg_loc : location IntMap.t }`
  - `location = Reg of string | Stack`
- 活跃区间分析 → 线性扫描分配物理寄存器（t0-t6, a0-a5, s0-s11）
- 寄存器不够时 spill 最远使用的 vreg

### 10. 代码生成 (`codegen.ml`)

SSA 形式 IR → RISC-V 汇编。接收 `alloc_result` 和 `live_var.liveness` 两项后端分析结果。

- **函数签名**：`generate : module_ -> Regalloc.alloc_result -> (string * Live_var.liveness) list -> string`
- **指令选择**：IR 指令模式匹配到 RISC-V 指令
  - `%rd = add %rs1, Imm(n)` → 若 n ∈ [-2048,2047]：`addi rd, rs1, n`
  - `%rd = icmp slt %rs1, %rs2` → `slt rd, rs1, rs2`
  - `call @f(args)` → 参数传 `a0-a7`，`jal f`，返回值取 `a0`
  - `ret %v` → `mv a0, %v; ret`
- **栈帧**：函数序言/尾声管理 `sp`、`ra` 保存、spill 槽位
- **Phi 处理**：边界插入 copy 块保证并行语义
- **数据段**：全局常量/变量输出 `.section .rodata` / `.data`
- **寄存器分配集成**：通过 `alloc_result` 参数，已分配物理寄存器的 vreg 直接运算，spill 的走栈
- **活变量集成**：通过 `liveness` 参数，可判定 vreg 在指令后是否死亡，用于省去对已死 vreg 的 spill store



---

## 文件结构

```
26_whu_compiler/
├── dune-project
├── lib/
│   ├── dune
│   ├── Common.ml              # 基础类型 (typ, binary_op, unary_op)
│   ├── AST.ml                  # 抽象语法树
│   ├── Symtab.ml               # 符号表（嵌套作用域）
│   ├── lexer.mll               # 词法分析 (ocamllex)
│   ├── parser.mly              # 语法分析 (Menhir)
│   ├── semant.ml               # 语义分析
│   ├── irgen.ml                # AST → IR 翻译 (待写)
│   ├── mem2reg.ml              # 内存模型 → SSA 提升 (待写)
│   ├── dominance.ml            # 支配树分析
│   ├── loop_info.ml            # 循环识别
│   ├── live_var.ml             # 活变量分析
│   ├── live_intervals.ml       # 活跃区间分析
│   ├── regalloc.ml             # 寄存器分配 (待写)
│   ├── codegen.ml              # IR → RISC-V 代码生成
│   ├── IR/
│   │   ├── Ir_types.ml         #   ir_type, instr, terminator, func, module
│   │   ├── Ir_print.ml         #   IR 文本打印
│   │   ├── Ir_analysis.ml      #   def-use 分析, replace_all_uses
│   │   └── Ir_builder.ml       #   IR 构造器
│   └── opt/
│       ├── const_fold.ml       #   常量折叠 (待写)
│       ├── const_prop.ml       #   常量传播 (待写)
│       ├── reassociate.ml      #   重关联 (待写)
│       ├── algebraic.ml        #   代数化简 (待写)
│       ├── copy_prop.ml        #   Copy 传播 (待写)
│       ├── cse.ml              #   公共子表达式消除 (待写)
│       ├── dce.ml              #   死代码消除 (待写)
│       ├── sccp.ml             #   稀疏条件常量传播 (待写)
│       ├── branch_fold.ml      #   无用分支消除 (待写)
│       ├── licm.ml             #   循环不变量外提 (待写)
│       ├── loop_unroll.ml      #   循环展开 (待写)
│       ├── dead_store_elim.ml  #   死 store 消除 (待写)
│       └── tail_call_elim.ml   #   尾递归消除 (待写)
└── test/
    └── *.c                     # ToyC 测试用例
```
