# ToyC 语言文法（开始符号：CompUnit）
```
CompUnit          → (Decl | FuncDef)+

Decl              → ConstDecl | VarDecl
ConstDecl         → "const" "int" ID "=" Expr ";"
VarDecl           → "int" ID "=" Expr ";"

Stmt              → Block
                  | ";"
                  | Expr ";"
                  | ID "=" Expr ";"
                  | Decl
                  | "if" "(" Expr ")" Stmt ("else" Stmt)?
                  | "while" "(" Expr ")" Stmt
                  | "break" ";"
                  | "continue" ";"
                  | "return" Expr? ";"

Block             → "{" Stmt* "}"

FuncDef           → ("int" | "void") ID "(" (Param ("," Param)*)? ")" Block
Param             → "int" ID

Expr              → LOrExpr
LOrExpr           → LAndExpr | LOrExpr "||" LAndExpr
LAndExpr          → RelExpr | LAndExpr "&&" RelExpr
RelExpr           → AddExpr | RelExpr ("<" | ">" | "<=" | ">=" | "==" | "!=") AddExpr
AddExpr           → MulExpr | AddExpr ("+" | "-") MulExpr
MulExpr           → UnaryExpr | MulExpr ("*" | "/" | "%") UnaryExpr
UnaryExpr         → PrimaryExpr | ("+" | "-" | "!") UnaryExpr
PrimaryExpr       → ID
                  | NUMBER
                  | "(" Expr ")"
                  | ID "(" (Expr ("," Expr)*)? ")"
```

# ToyC 语言终结符特征
1. **标识符 ID**
正则表达式：`[_A-Za-z][_A-Za-z0-9]*`，遵循C语言标识符规范。

2. **整数 NUMBER**
正则表达式：`-?(0|[1-9][0-9]*)`，仅支持十进制整数字面量。

3. **空白字符与注释**
- 空白字符：空格、制表符、换行等，全部忽略；
- 单行注释：`//` 开头，至本行末尾结束；
- 多行注释：`/*` 开头，匹配最近的 `*/` 结束，与C注释规则一致。

# ToyC 语言语义约束
## 程序结构
1. 程序可包含全局常量声明、全局变量声明、函数定义；
2. 必须存在入口函数：名为 `main`、无参数、返回值类型 `int`。

## 函数规则
1. 形参仅支持 `int` 类型，返回值为 `int` / `void`；
2. 函数只能定义在全局作用域，函数名全局唯一，不可嵌套定义；
3. 函数不能作为值参与运算、赋值、传递；
4. 函数调用必须出现在该函数定义之后（允许递归自调用）；
5. `int` 返回函数：所有执行路径必须通过 `return` 返回整型表达式；
6. `void` 返回函数：可无return；若写return则不能携带返回表达式。

## 语句规则
语句类型：代码块、空语句、表达式语句、变量赋值、块内声明、if-else、while循环、break、continue、return；
1. 无返回值的函数调用不能作为if/while条件、赋值右值；
2. break、continue仅允许出现在while循环内部；
3. 语法、执行语义与标准C保持一致。

## 变量与常量声明
1. 仅支持 `int` 32位有符号整数类型；区分全局/局部变量、全局/局部const常量；
2. 所有变量、常量声明**必须附带初始化表达式**；
3. const常量约束：
   - 初始化表达式仅允许数字字面量、已前置声明的常量，以及二者组合的算术/逻辑运算；
   - 编译期即可算出常量值；
   - 常量不可被赋值修改，不能出现在赋值左值位置。
4. 作用域与生命周期：
   - 全局变量/常量：程序全程有效；
   - 局部变量/常量：从声明行开始，至当前代码块结束销毁；
   - 函数形参：函数体内可见；
   - `{}` 代码块创建嵌套作用域，内层同名标识符屏蔽外层；
   - 变量、常量、形参必须先声明后使用。

## 表达式规则
1. 支持：逻辑运算、关系运算、加减乘除模算术、括号、标识符引用、字面量、函数调用；
2. 运算符优先级、结合性、求值规则同C语言；
3. `&&`、`||` 遵循短路求值；
4. 真值规则：非零为真，0为假；
5. 运行时禁止除数为0。
